<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.green.camping.dao.user.MemberMapper">
  
  	<select id="login" resultType="kr.green.camping.vo.user.JoinVO">
		select * from account where id = #{id} and pw = #{pw}
	</select>
	
	<insert id="join" >
		insert into account (id, name, gender, pw, phone, birth, email, postcode, address, admin) 
		values (#{join.id}, #{join.name}, #{join.gender}, #{join.pw}, #{join.phone}, #{join.birth}, #{join.email}, #{join.postcode}, #{join.address}, #{join.admin})
	</insert>
	
	<select id="loginById" resultType="kr.green.camping.vo.user.JoinVO">
		select * from account where id = #{id}
	</select>
  	
	<select id="chkDupId" parameterType="String" resultType="Integer">
      	select count(*) from account where id = #{id}
	</select>
	
	
</mapper>