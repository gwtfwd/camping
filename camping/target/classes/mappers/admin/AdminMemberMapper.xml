<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.green.camping.dao.admin.AdminMemberMapper">
  
	<insert id="adminJoin" >
		insert into account (id, name, pw, phone, email, admin) 
		values (#{adminJoin.id}, #{adminJoin.name}, #{adminJoin.pw}, #{adminJoin.phone}, #{join.email}, #{adminJoin.admin})
	</insert>
	
	<select id="adminById" resultType="kr.green.camping.vo.admin.AdminJoinVO">
		select * from admin_account where id = #{adminId}
	</select>
	
	
</mapper>